'use client'

import { PostCreateForm } from "@/components/admin/forms/post-create-form";

export function CreatePostPage() {
    return (
        <main>
            <PostCreateForm/>
        </main>
    );
}
